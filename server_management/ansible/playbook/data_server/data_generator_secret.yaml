- name: application-secret.yaml 배포
  hosts: data_instance_server
  tasks:
    - name: secret 폴더생성
      file:
        path: ~/secret
        state: directory

    - name: application-secret.yml 파일 생성
      template:
        src: ./secret/application-secret.yml.j2  # 템플릿 파일의 경로
        dest: ~/sercret/application-secret.yml  # 생성할 파일의 경로