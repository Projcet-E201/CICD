version: "3"
services:
  # back - blue
  dev-back-blue:
    container_name: dev-data-blue
    build:
      context: .
      dockerfile: Dockerfile
      args:
        PROFILE: "devblue"
    ports:
      - "8097:8097"
    networks:
      - br_log
      - br_app

networks:
  br_log:
    external: true
  br_app:
    external: true
